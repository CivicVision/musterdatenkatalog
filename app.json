{
  "env": {
    "SECRET_TOKEN": {
      "description": "A secret key for verifying the integrity of signed cookies.",
      "generator": "secret"
    }
  },
  "environments": {
    "review": {
      "scripts": {
        "postdeploy": "python manage.py loaddata initial_data && python manage.py loaddata admin_data"
      }
    }
  }
}
